# ── secrets & local config ────────────────────────────────────────────────────
.env
.env.backup*
.env.example.backup*
.env.*.backup*
tmp/

# ── generated files (built from .env by start.sh / generate-olla-config.sh) ──
# Contains LAN node IPs — must not be committed to a public repo
olla.yaml

# ── runtime / logs ────────────────────────────────────────────────────────────
logs/

# ── Python
__pycache__/
*.pyc
*.pyo

# ── Editor
.vscode/
.idea/
*.swp
*.swo

# ── OS
.DS_Store
Thumbs.db

# ── Logs
*.log

# Obsidian workspace config (local + auto-generated by install.sh)
.obsidian/
